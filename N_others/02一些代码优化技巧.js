//1.if(animol=='dog' || animol=='cat')
//可以创造enum或者数组保存animol状态：
let animols=[];
animols.includes('dog')
//2.判断对象中是否含有某属性：不需要写冗余的ifelse:解构
let cat={
    name:'huahua',
    age:3
}
const fn=function({name,age}){
    if(name==undefined){}

} 
//3.switch-case写成 对象拿相应key

